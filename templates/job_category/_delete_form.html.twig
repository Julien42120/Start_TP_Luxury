<form method="post" action="{{ path('job_category_delete', {'id': job_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_category.id) }}">
    <button class="btn bn-lg gradient red darken-3 btn-block waves-effect waves-light btn-register">Delete</button>
</form>
