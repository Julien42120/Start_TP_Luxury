{% extends 'base.html.twig' %}

{% block title %}JobTypes{% endblock %}

{% block body %}
    <h1 style="padding-top:10rem; text-align:center; background-color:#2F3138; margin-top:0; color:white ;padding-bottom:2%">JobTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_type.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ job_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn bn-lg grey darken-1 btn-block waves-effect waves-light btn-register"  href="{{ path('job_types_index') }}">back to list</a>

    <a style="margin:0" class="btn bn-lg gradient secondary btn-block waves-effect waves-light btn-register" href="{{ path('job_types_edit', {'id': job_type.id}) }}">edit</a>

    {{ include('job_types/_delete_form.html.twig') }}
{% endblock %}
